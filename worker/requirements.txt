# Loose bounds until the first successful install; then freeze:
#   pip freeze > worker/requirements.txt
# google-adk was 2.7.1 at scaffold time (Aug 26 2026). Pin the exact version
# once it installs cleanly on Python 3.12 — this is the dependency most likely
# to move under you mid-build.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
httpx>=0.27,<1.0
pyjwt[crypto]>=2.8,<3.0
google-adk>=2.7,<3.0
google-cloud-firestore>=2.16,<3.0
google-auth>=2.30,<3.0
